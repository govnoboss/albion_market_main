# GBot Roadmap: Optimization, Security & Updates

## 📍 1. Настройка Nuitka (Dual Profile Build System)
**Цель:** Создать воспроизводимую среду сборки с двумя профилями: DEV (быстро) и PROD (качественно/защищено).

### 1.1 Анализ и Подготовка
- [ ] Составить список **INCLUDE** (критичные библиотеки: `cv2`, `numpy`, `PIL`, `pyautogui`, `requests`, `cryptography`, `pydantic`).
- [ ] Составить список **EXCLUDE** (bloatware: `matplotlib`, `pandas`, `scipy`, `unittest`, `tkinter`, `pydoc`).
- [ ] Убедиться что tesseract будет входить в сборку, пользователь не должен сам его качать.

### 1.2 Профиль DEV (Debug Build)
*Приоритет: Скорость компиляции (< 1 мин), удобство отладки.*
- [ ] Описать батник `build_dev.bat`:
    - `--standalone` (папка с файлами, без архивации — очень быстро).
    - `--enable-console` (видим принты и ошибки).
    - `--no-progress` (меньше шума).
    - `--follow-imports` (чтобы всё работало из коробки).

### 1.3 Профиль PROD (Release Build)
*Приоритет: Защита кода, малый размер (< 50-70 МБ), отсутствие консоли.*
- [ ] Описать батник `build_release.bat`:
    - `--onefile` (всё в одном .exe).
    - `--windows-disable-console` (без черного окна).
    - `--enable-plugin=anti-bloat` (удаление мусора).
    - `--enable-plugin=upx` (сжатие .exe, если UPX установлен).
    - `--lto=yes` (Link Time Optimization — удаляет неиспользуемый код C).
    - Настроить `--nofollow-imports` для стандартных библиотек (максимальное уменьшение размера).
    - Добавить `--windows-icon` и `--product-version` для солидности.

---

## 📍 2. Проверка Версий (Version Check)
**Цель:** Научить PROD сборку понимать, что она устарела.

### 2.1 Логика
- [ ] `src/core/version.py`: Хранит константу `CURRENT_VERSION = "1.0.0"`.
- [ ] `check_update()`: Делает GET запрос к GitHub API `repos/.../releases/latest`.
- [ ] Сравнивает `tag_name` (например, `v1.0.1`) с локальной версией.

### 2.2 UI Интеграция
- [ ] При старте (асинхронно) проверять обновление.
- [ ] Если есть: Показать кнопку "Обновить" (мигает/светится) в главном меню или футере.
- [ ] (Опционально) Блокировать работу, если эндпоинт поменялся (Critical Update).

---

## 📍 3. Система Самообновления (Self-Update)
**Цель:** Скачать и заменить PROD build "на лету".

### 3.1 Механизм (Swap)
- [ ] Скачать новый `GBot_v1.0.1.exe` в папку `%TEMP%` или рядом как `GBot.new`.
- [ ] Проверить хеш/размер (целостность).
- [ ] Перезапуск через `updater.bat`:
    1.  `taskkill /F /IM GBot.exe` (убить текущий процесс).
    2.  `timeout 1` (подождать).
    3.  `move /Y GBot.new GBot.exe` (заменить файл).
    4.  `start GBot.exe` (запустить заново).

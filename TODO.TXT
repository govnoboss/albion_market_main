═══════════════════════════════════════════════════
🔴 КРИТИЧНО — Баги и срочные фиксы
═══════════════════════════════════════════════════

1. Профиты роняют программу при первом запуске — исправить краш.
2. Поставить тему фона для городов закупки/продажи — белый текст на белом фоне.
3. При запуске программы выводить в фокус — сейчас остается сзади.
4. Удалить дубликат импорта в settings_panel.py (строка 12 дублирует 11).
5. Удалить мёртвые комментарии разработчика в settings_panel.py (строки 282-284).
6. Удалить legacy-код (src/legacy/ — не используется).
7. Убрать отладочные sys.stderr.write("DEBUG:...") из main.py.

═══════════════════════════════════════════════════
🟡 ВАЖНО — Надёжность и UX
═══════════════════════════════════════════════════

8.  Уменьшить скриншоты подсказки координат. Добавить возможность менять масштаб.
9.  Вместо ПКМ использовать кнопку для подтверждения координат.
10. Лог тяжело масштабировать — слишком маленькая кнопка взятия, увеличить.
11. Калибровка тиров через настройки — добавить 1 точку вниз при отрисовке (добавился Тир = Все).
12. Атомарная запись prices.json — писать во временный файл → os.replace() чтобы крэш не убил данные.
13. Thread-safety для ConfigManager — добавить threading.Lock() (доступ из main + bot thread).

═══════════════════════════════════════════════════
🟢 УЛУЧШЕНИЯ — Архитектура и стиль
═══════════════════════════════════════════════════

14. Вынести inline-стили из UI-компонентов в styles.py (settings_window, settings_panel, buyer_window).
15. Унифицировать логирование — заменить print()/sys.stderr.write() на get_logger() везде.

═══════════════════════════════════════════════════
🔵 ФИЧИ — Новый функционал
═══════════════════════════════════════════════════

16. Статистика покупок — логирование в data/purchases.json (предмет, тир, кол-во, цена, время, город).
17. Унификация горячих клавиш Buyer — F5/F6 работают как в Scanner.
18. Windows-уведомления при критических ошибках (OCR fail, потеря соединения) — toast notification.

═══════════════════════════════════════════════════
⚪ ТЕСТЫ И DEVOPS
═══════════════════════════════════════════════════

[x] Unit-тесты: price_storage, config, updater, ocr, interaction.
20. CI/CD — GitHub Actions для линтера и тестов при коммите.
[x] Предсборочная валидация в Release Manager — блокировать релиз если тесты не прошли.
[x] Переместить кнопку Меню в левый верхний угол (Scanner & Buyer).
[x] Добавить иконку приложения (assets/icon.png -> .ico в build.bat).
[x] Исправить иконку в панели задач (AppUserModelID).


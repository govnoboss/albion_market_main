1. Логи баера работают неверно. 
2. Локализировать Куплено шт в мини меню и логах.
3. Добавить мастер настройки только для ЧР.

4. [Архитектура] Интеграция Albion Data API (публичный API):
   - Использовать как "радар" для поиска общих трендов и арбитража между городами в фоновом режиме.
   - Плюсы: Проще в разработке (JSON GET запросы), нет нужды в OCR, получение данных по всем городам сразу, есть история цен.
   - Минусы: Данные могут быть устаревшими (особенно на редкие товары), есть лимиты на количество запросов к API.

5. [Архитектура] Локальный перехват сетевых пакетов (Packet Sniffing) через Python:
   - Использовать как ультимативную замену OCR для мгновенного и точного чтения цен при открытом окне рынка.
   - Плюсы: 100% свежие цены без задержек (сразу после ответа сервера), нет лимитов API, 100% точность чтения (нет ошибок машинного зрения), работает, даже если окно игры свернуто/перекрыто.
   - Минусы: Сложность реализации, зависимость от драйверов (Npcap), парсер ломается при крупных обновлениях игры (нужно обновлять OpCodes), требует осторожности, если комбинировать с большим числом автоматических действий.
   - Идеальный комбинированный подход: бот эмулирует клики человека (листает рынок) -> сниффер пакетов ("прицел") моментально и безошибочно читает цены -> бот принимает решение.

6. [Сниффер] Фаза 5: Постоянное хранение данных (SQLite):
   - Создать `sniffer/market_db.py` для управления базой данных `market_data.db`.
   - Реализовать таблицы:
     * `market_orders`: хранение текущих лотов (ItemId, Price, Amount, Quality, Enchantment, OrderId).
     * `market_history`: хранение данных графиков (ItemId, Price, Timestamp).
     * `gold_prices`: хранение курса золота.
   - Добавить дедупликацию: не сохранять один и тот же OrderId дважды.
   - Интегрировать `MarketDB` в `sniffer_poc.py` для автоматического сохранения в фоне.
   - Написать скрипт-экспортер `sniffer/export_to_csv.py` для анализа данных в Excel.
   - [Опционально] Добавить логику Price Alerts (уведомления о дешевых лотах).

# ⌨️ Справочник консольных команд (CLI Guide)

В этом документе собраны все основные команды, необходимые для разработки, тестирования и сборки проекта.

---

## 🚀 Запуск приложения (Client)

Для ежедневной разработки и проверки изменений:

```powershell
# Основной запуск (Launcher -> App)
python src/main.py

# Быстрая проверка UI и логики лицензии (без полного запуска)
python tests/run_test.py
```

---

## 🛠 Сборка и упаковка (Build)

Команды для создания готового `.exe` и его упаковки:

```powershell
# 1. Скомпилировать в .exe (Standalone)
.\build.bat

# 2. Упаковать в ZIP с Tesseract и ресурсами
.\package.bat
```

---

## 🔑 Безопасность и Лицензии (Tools)

Инструменты для управления ключами и лицензированием:

```powershell
# Генерация новых RSA-ключей (обновит src/core/license.py автоматически)
python tools/generate_keys.py

# Проверка соответствия пары ключей
python tools/check_key_pair.py

# Верификация работы Heartbeat (системы "пульса")
python tools/verify_heartbeat.py

# Полная проверка безопасности системы лицензий
python tools/verify_security.py
```

---

## 🌐 Серверная часть (Server)

Команды для управления сервером лицензий:

```powershell
# Запуск сервера локально (для отладки)
cd server
pip install -r requirements.txt
python main.py

# Деплой сервера на Fly.io (PowerShell)
.\tools\deploy_server.ps1
```

---

## 🧪 Тестирование (Tests)

Проект использует как кастомные скрипты, так и стандартные тесты:

```powershell
# Проверка логики лицензирования
python tests/test_license.py

# Проверка OCR и Template Matching (распознавание картинок)
python tests/test_template_matching.py

# Проверка JWT сессий
python tests/test_jwt_session.py

# Проверка защиты от частого обращения (Rate Limit)
python tests/test_rate_limit.py
```

---

## 📦 База данных (Database)

Миграции и управление данными:

```powershell
# Миграция базы данных сервера
python tools/migrate_db.py

# Проверка работы базы на продакшене
python tools/test_prod.py
```

---

## 💡 Советы по PowerShell
Если скрипт `.bat` или `.ps1` не запускается напрямую, добавьте `.\`:
```powershell
.\build.bat
```
Для работы всех команд `python` убедитесь, что виртуальная среда активирована:
```powershell
.\.venv\Scripts\Activate
```

# Руководство Администратора: Система Лицензирования GBot

## 1. Как это работает (Кратко)

Система лицензирования состоит из трех частей:
1.  **Бог (Сервер):** Работает в облаке (Fly.io). Генерирует и проверяет ключи. Хранит базу данных.
    *   *Адрес:* `https://gbot-license.fly.dev`
2.  **Клиент (Бот):** Программа у пользователя. При запуске спрашивает у сервера: "Можно мне работать?".
3.  **Защита (RSA + NTP):**
    *   Сервер подписывает каждый ответ своим **Секретным Ключом**.
    *   Бот проверяет подпись с помощью встроенного **Публичного Ключа**.
    *   Бот сверяет время через интернет, чтобы пользователь не мог "отмотать" дату назад.

Если хакер попытается подделать сервер или время — бот откажется работать.

---

## 2. Ваши обязанности как Админа

### А. Управление Ключами (Ежедневная работа)
Все управление происходит через **Админ-Панель**:
*   **Ссылка:** `https://gbot-license.fly.dev/admin/`
*   **Логин/Пароль:** Тот, который задан в переменных среды (по умолчанию `admin`, рекомендуется сменить).

**Что можно делать:**
1.  **Генерировать ключи:** Создавать пачки ключей на 30, 90 дней и т.д.
2.  **Банить пользователей:** Кнопка "Deactivate" или "Delete" напротив ключа.
3.  **Сбрасывать привязку:** Если пользователь сменил ПК, нужно найти его ключ и сбросить HWID (в текущей версии удалением/перевыдачей, либо через базу данных).

### Б. Обслуживание Сервера (Раз в месяц/год)
Сервер работает автономно. Ваша задача — следить, чтобы он был доступен.
*   **Оплата Fly.io:** У них есть бесплатный тариф, но если нагрузка вырастет, возможно потребуется привязать карту (около $5/мес за минимальный платный инстанс).
*   **База Данных:** Хранится на постоянном диске (Volume) в облаке. Она не пропадет при перезагрузке.

### В. Безопасность (Критично!)
У вас есть **Приватный Ключ** (`keys/private.pem` на вашем ПК).
*   🔴 **НИКОМУ** не давайте этот файл.
*   🔴 Не загружайте его в публичные репозитории.
*   Если ключ утечет:
    1.  Хакеры смогут сделать "пиратский" сервер.
    2.  Вам придется генерировать новые ключи, обновлять сервер и **пересобирать всех ботов** с новым публичным ключом.

---

## 3. Инструкции (How-To)

### Как обновить сервер?
Если вы внесли изменения в код сервера, запустите скрипт автоматического обновления:
```powershell
./tools/deploy_server.ps1
```
Он сам залогинится, проверит ключи и обновит программу в облаке.

### Как создать нового бота для пользователей?
1.  Убедитесь, что в `src/core/license.py` прописан актуальный `PUBLIC_KEY_PEM`.
2.  Запустите сборку:
    ```powershell
    build_gbot.bat
    ```
3.  Отправляйте пользователям новый `.exe`. Старые версии бота могут перестать работать при обновлении протокола безопасности.

### Как сменить пароль Админа?
Пароль задается в настройках Fly.io.
```powershell
fly secrets set ADMIN_PASSWORD="НовыйСложныйПароль"
```
После этого сервер перезагрузится сам.

### Что если сервер упал?
1.  Проверьте статус: `fly status`
2.  Посмотрите логи: `fly logs`
3.  Перезагрузите принудительно: `fly restart`

---

## 4. Чек-лист перед продажей
Перед тем как начать продавать ключи:

- [ ] **Сменить пароль админа** на сложный.
- [ ] **Сделать бэкап** `private.pem` на флешку или в надежное место.
- [ ] **Протестировать** покупку и активацию на чистом ПК.
- [ ] Убедиться, что `tools/test_prod.py` показывает `SUCCESS`.

---

*Документ создан: 09.02.2026*
